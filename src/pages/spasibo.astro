---
import BaseLayout from "../layouts/BaseLayout.astro";
import Section from "../components/ui/Section.astro";
import Button from "../components/ui/Button.astro";

const meta = {
  title: "Спасибо за заявку — 1001 Мебель",
  description: "Ваша заявка успешно отправлена",
  canonical: "/spasibo",
  noindex: true
};
---
<BaseLayout {...meta}>
  <Section class="min-h-[80vh] flex items-center justify-center text-center" padding="none" container={false}>
    <div class="max-w-xl px-4 reveal-up">
       <div class="w-20 h-20 rounded-full border border-accent flex items-center justify-center mx-auto mb-8 text-accent">
          <svg width="32" height="32" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
             <path d="M20 6L9 17L4 12" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
       </div>
       
       <span class="text-accent text-xs font-bold tracking-[0.2em] uppercase block mb-6">Заявка принята</span>
       <h1 class="font-serif text-4xl md:text-5xl text-white mb-6">Спасибо за доверие</h1>
       <p class="text-lg text-muted mb-10 leading-relaxed">
         Мы получили вашу заявку. Наш эксперт свяжется с вами в течение 15 минут (в рабочее время) для уточнения деталей.
       </p>
       
       <Button href="/">Вернуться на главную</Button>
    </div>
  </Section>
</BaseLayout>
