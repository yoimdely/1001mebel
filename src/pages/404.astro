---
import BaseLayout from "../layouts/BaseLayout.astro";
import Section from "../components/ui/Section.astro";
import Button from "../components/ui/Button.astro";

const meta = {
  title: "Страница не найдена — 1001 Мебель",
  description: "Ошибка 404",
  canonical: "/404",
  noindex: true
};
---
<BaseLayout {...meta}>
  <Section class="min-h-[80vh] flex items-center justify-center relative overflow-hidden" padding="none" container={false}>
    <div class="absolute inset-0 flex items-center justify-center pointer-events-none select-none overflow-hidden">
       <span class="font-serif text-[20vw] text-white/5 leading-none">404</span>
    </div>

    <div class="relative z-10 text-center max-w-2xl px-4 reveal-up">
      <div class="text-accent text-xs font-bold tracking-[0.2em] uppercase mb-6">Ошибка 404</div>
      <h1 class="font-serif text-4xl md:text-6xl text-white mb-8">Страница <br /> не найдена</h1>
      <p class="text-xl text-muted mb-12 leading-relaxed">
        Кажется, страница, которую вы ищете, переехала в другой интерьер или никогда не существовала.
      </p>
      <div class="flex justify-center gap-4 flex-wrap">
        <Button href="/">На главную</Button>
        <Button href="/kontakty" variant="secondary">Контакты</Button>
      </div>
    </div>
  </Section>
</BaseLayout>
